# Development Settings (for AIVM devnet)
AIVM_DEVNET_HOST=localhost
AIVM_DEVNET_PORT=50050
AIVM_DEVNET_ENABLED=true
AIVM_LOG_LEVEL=INFO

# Node Configuration
NODE_ID=default_node
NODE_HOST=localhost
NODE_PORT=50050

# Network Configuration (only needed for production)
# NILLION_NETWORK_URL=http://localhost:50050
# NILLION_NODE_KEY=your_node_key_here
# NILLION_NETWORK_KEY=your_network_key_here

# Security Settings (Optional)
SSL_ENABLED=false
